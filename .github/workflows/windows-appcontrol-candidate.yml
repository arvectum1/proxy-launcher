name: Windows App Control candidate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, 'release/**', 'fix/**']
  workflow_dispatch:

concurrency:
  group: windows-appcontrol-candidate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  candidate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.10'

      - name: Install pinned Inno Setup 6.7.1
        shell: pwsh
        run: choco install innosetup --version=6.7.1 --no-progress -y

      - name: Validate post-incident contracts and PowerShell syntax
        shell: pwsh
        run: |
          foreach ($script in @(
            'tools/build_windows_appcontrol_static_runtime.ps1',
            'tools/build_windows_appcontrol_recovery.ps1',
            'tools/build_windows_appcontrol_installer.ps1',
            'tools/windows_app_control_provisional_trust_pack.ps1',
            'tools/windows_app_control_enterprise_trust_pack_v2.ps1',
            'tools/windows_app_control_enforced_readiness.ps1',
            'tools/windows_app_control_candidate_stand.ps1',
            'tools/windows_app_control_candidate_runner.ps1',
            'qa/windows_appcontrol_candidate_e2e.ps1'
          )) {
            [void][scriptblock]::Create((Get-Content -LiteralPath $script -Raw))
            Write-Host "PowerShell syntax PASS: $script"
          }
          python -m py_compile tools/extract_pyinstaller_onefile_runtime.py
          python -m unittest `
            tests.test_windows_app_control_post_incident_hardening `
            tests.test_windows_app_control_enterprise_candidate `
            tests.test_windows_app_control_trust_pack_v2 `
            tests.test_windows_app_control_candidate_runner `
            tests.test_portable_lifecycle_static_runtime `
            -v

      - name: Build canonical portable license source
        shell: pwsh
        run: |
          ./tools/clean_build_windows.ps1 -PythonExecutable python
          $zip = Get-ChildItem -LiteralPath out -Filter '*-windows-x64-portable.zip' -File | Select-Object -First 1
          if (-not $zip) { throw 'Canonical portable ZIP was not produced.' }
          ./tools/windows_promoted_license_compliance.ps1 -PortableZip $zip.FullName

      - name: Prepare immutable 0.2.2 P0.4 onefile trust inputs without execution
        id: historical
        shell: pwsh
        run: |
          $zip = (Resolve-Path 'release\Arvectum-Proxy-Launcher-Windows-0.2.2-P0.4-client.zip').Path
          $expectedZip = 'd0de0dcea1814871de104981aa4f8ff4b5da6be143b334699741001b70fc6b0a'
          $actualZip = (Get-FileHash -LiteralPath $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          if ($actualZip -ne $expectedZip) { throw 'Immutable 0.2.2 P0.4 ZIP SHA256 mismatch.' }
          $package = (Join-Path $PWD 'out\historical-0.2.2-p0.4-package')
          $runtime = (Join-Path $PWD 'out\historical-0.2.2-p0.4-onefile-runtime')
          Remove-Item -LiteralPath $package,$runtime -Recurse -Force -ErrorAction SilentlyContinue
          Expand-Archive -LiteralPath $zip -DestinationPath $package -Force
          $exe = Join-Path $package 'Arvectum Proxy Launcher.exe'
          if (-not (Test-Path -LiteralPath $exe -PathType Leaf)) { throw 'Historical 0.2.2 Launcher is missing after extraction.' }
          $expectedExe = '7ef02652e31bbbd68833be599135cf59519c42b1f8a8febb580b3891ffc35ec0'
          if ((Get-FileHash -LiteralPath $exe -Algorithm SHA256).Hash.ToLowerInvariant() -ne $expectedExe) {
            throw 'Historical 0.2.2 Launcher SHA256 mismatch.'
          }
          & '.build-venv\Scripts\python.exe' 'tools\extract_pyinstaller_onefile_runtime.py' `
            --input $exe --output $runtime
          if ($LASTEXITCODE -ne 0) { throw 'Historical onefile runtime extraction failed.' }
          $inventory = Get-Content (Join-Path $runtime 'onefile-runtime.json') -Raw | ConvertFrom-Json
          if ([string]$inventory.result -ne 'PASS' -or [bool]$inventory.executed_input) {
            throw 'Historical onefile runtime inventory is not safe/PASS.'
          }
          if ([int]$inventory.executable_member_count -lt 2) { throw 'Historical onefile native runtime inventory is unexpectedly empty.' }
          "package_dir=$((Resolve-Path $package).Path)" >> $env:GITHUB_OUTPUT
          "runtime_dir=$((Resolve-Path $runtime).Path)" >> $env:GITHUB_OUTPUT

      - name: Build synthetic predecessor lifecycle fixture
        id: predecessor
        shell: pwsh
        run: ./tools/build_windows_installer.ps1 -PythonExecutable python -SyntheticPredecessor

      - name: Build App Control enterprise candidate
        id: candidate
        shell: pwsh
        run: |
          ./tools/build_windows_appcontrol_installer.ps1 -PythonExecutable '.build-venv\Scripts\python.exe'
          $bundle = Get-ChildItem -LiteralPath out -Directory -Filter '*-windows-x64-appcontrol-setup-bundle' | Select-Object -First 1
          if (-not $bundle) { throw 'App Control candidate bundle directory was not produced.' }
          $setupDir = Join-Path $bundle.FullName 'setup'
          $setup = Get-ChildItem -LiteralPath $setupDir -Filter '*.exe' -File | Select-Object -First 1
          $data = @(Get-ChildItem -LiteralPath $setupDir -Filter '*.bin' -File)
          if (-not $setup) { throw 'App Control candidate Setup EXE is missing.' }
          if ($data.Count -lt 1) { throw 'App Control candidate Setup BIN is missing.' }
          $zip = Get-ChildItem -LiteralPath out -Filter '*-windows-x64-appcontrol-setup-bundle.zip' -File | Select-Object -First 1
          if (-not $zip) { throw 'App Control candidate ZIP is missing.' }
          "setup_path=$($setup.FullName)" >> $env:GITHUB_OUTPUT
          "bundle_dir=$($bundle.FullName)" >> $env:GITHUB_OUTPUT
          "bundle_zip=$($zip.FullName)" >> $env:GITHUB_OUTPUT
          "bundle_name=$($zip.Name)" >> $env:GITHUB_OUTPUT

      - name: Fresh repair migration uninstall lifecycle and deterministic uninstaller
        shell: pwsh
        run: |
          ./qa/windows_appcontrol_candidate_e2e.ps1 `
            -CandidateSetup '${{ steps.candidate.outputs.setup_path }}' `
            -PredecessorSetup '${{ steps.predecessor.outputs.predecessor_setup_path }}' `
            -EvidencePath 'out\windows-appcontrol-candidate-e2e.json'

      - name: Build rehearsal-only provisional App Control supplemental
        id: provisional
        shell: pwsh
        run: |
          ./tools/windows_app_control_provisional_trust_pack.ps1 `
            -EnterpriseBundleDirectory '${{ steps.candidate.outputs.bundle_dir }}' `
            -BasePolicyId 'DC1C604C-46EA-40B7-9F47-CF582B225D5E' `
            -NormalLifecycleEvidencePath 'out\windows-appcontrol-candidate-e2e.json' `
            -HistoricalPackageDirectory '${{ steps.historical.outputs.package_dir }}' `
            -HistoricalRuntimeDirectory '${{ steps.historical.outputs.runtime_dir }}' `
            -OutputDirectory 'out\app-control-provisional-trust'
          $manifest = Get-Content 'out\app-control-provisional-trust\provisional-trust-pack.json' -Raw | ConvertFrom-Json
          if ([bool]$manifest.enforced_lifecycle_ready) { throw 'Provisional policy illegally claims Enforced readiness.' }
          if ([bool]$manifest.final_acceptance_evidence) { throw 'Provisional policy illegally claims final acceptance.' }
          if ([bool]$manifest.historical_onefile_input_executed_during_trust_build) { throw 'Historical onefile input was executed during trust build.' }
          if ([int]$manifest.historical_onefile_runtime_executable_count -lt 2) { throw 'Historical onefile native runtime trust is missing.' }
          $cip = Join-Path 'out\app-control-provisional-trust' ([string]$manifest.supplemental_policy_cip)
          if (-not (Test-Path -LiteralPath $cip -PathType Leaf)) { throw 'Provisional CIP is missing.' }
          "manifest_path=$((Resolve-Path 'out\app-control-provisional-trust\provisional-trust-pack.json').Path)" >> $env:GITHUB_OUTPUT
          "policy_id=$([string]$manifest.supplemental_policy_id)" >> $env:GITHUB_OUTPUT
          "cip_path=$((Resolve-Path $cip).Path)" >> $env:GITHUB_OUTPUT

      - name: Assemble self-contained ARVECTUM-DEMO stand kit
        id: standkit
        shell: pwsh
        run: |
          $kit = Join-Path $PWD 'out\APL-WIN-014-0.2.4-stand-kit'
          Remove-Item -LiteralPath $kit -Recurse -Force -ErrorAction SilentlyContinue
          foreach ($dir in @('candidate','provisional','historical-0.2.2-p0.4','evidence','kit-tools')) {
            New-Item -ItemType Directory -Path (Join-Path $kit $dir) -Force | Out-Null
          }

          Copy-Item -Path (Join-Path '${{ steps.candidate.outputs.bundle_dir }}' '*') -Destination (Join-Path $kit 'candidate') -Recurse -Force
          Copy-Item -Path (Join-Path (Resolve-Path 'out\app-control-provisional-trust').Path '*') -Destination (Join-Path $kit 'provisional') -Recurse -Force
          Copy-Item -Path (Join-Path '${{ steps.historical.outputs.package_dir }}' '*') -Destination (Join-Path $kit 'historical-0.2.2-p0.4') -Recurse -Force
          Copy-Item -LiteralPath 'out\windows-appcontrol-candidate-e2e.json' -Destination (Join-Path $kit 'evidence\windows-appcontrol-candidate-e2e.json') -Force
          Copy-Item -LiteralPath 'tools\windows_app_control_candidate_stand.ps1' -Destination (Join-Path $kit 'APL-WIN-014-STAND.ps1') -Force
          Copy-Item -LiteralPath 'tools\windows_app_control_candidate_runner.ps1' -Destination (Join-Path $kit 'APL-WIN-014.ps1') -Force
          Copy-Item -LiteralPath 'tools\windows_app_control_enterprise_trust_pack_v2.ps1' -Destination (Join-Path $kit 'kit-tools\windows_app_control_enterprise_trust_pack_v2.ps1') -Force
          Copy-Item -LiteralPath 'tools\windows_app_control_enforced_readiness.ps1' -Destination (Join-Path $kit 'kit-tools\windows_app_control_enforced_readiness.ps1') -Force

          $files = @(
            Get-ChildItem -LiteralPath $kit -File -Recurse -Force |
              Sort-Object FullName |
              ForEach-Object {
                $relative = $_.FullName.Substring($kit.Length).TrimStart('\').Replace('\','/')
                [ordered]@{
                  relative_path = $relative
                  size = [long]$_.Length
                  sha256 = (Get-FileHash -LiteralPath $_.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
                }
              }
          )
          $sourceCommit = if ('${{ github.event_name }}' -eq 'pull_request') { '${{ github.event.pull_request.head.sha }}' } else { '${{ github.sha }}' }
          $manifest = [ordered]@{
            schema = 'arvectum.proxy.apl-win-014-stand-kit.v1'
            task = 'APL-WIN-014'
            created_utc = [DateTime]::UtcNow.ToString('o')
            result = 'PASS'
            version = '0.2.4'
            source_commit = $sourceCommit
            build_checkout_commit = (git rev-parse HEAD).Trim()
            candidate_label = '0.2.4-appcontrol-candidate'
            promoted_release = $false
            preflight_is_non_destructive = $true
            execute_requires_separate_owner_action = $true
            base_policy_id = '{DC1C604C-46EA-40B7-9F47-CF582B225D5E}'
            files = $files
          }
          $manifestPath = Join-Path $kit 'stand-kit.json'
          $manifest | ConvertTo-Json -Depth 10 | Set-Content -LiteralPath $manifestPath -Encoding UTF8

          # Verify the manifest against the completed kit before zipping it.
          foreach ($file in @($manifest.files)) {
            $path = Join-Path $kit ([string]$file.relative_path)
            if (-not (Test-Path -LiteralPath $path -PathType Leaf)) { throw "Stand-kit file missing after assembly: $($file.relative_path)" }
            if ([long](Get-Item -LiteralPath $path).Length -ne [long]$file.size) { throw "Stand-kit size mismatch: $($file.relative_path)" }
            $sha = (Get-FileHash -LiteralPath $path -Algorithm SHA256).Hash.ToLowerInvariant()
            if ($sha -ne [string]$file.sha256) { throw "Stand-kit SHA256 mismatch: $($file.relative_path)" }
          }

          $zip = "$kit.zip"
          Remove-Item -LiteralPath $zip -Force -ErrorAction SilentlyContinue
          Compress-Archive -Path (Join-Path $kit '*') -DestinationPath $zip -Force
          $zipSha = (Get-FileHash -LiteralPath $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          Write-Host "Stand kit: $zip"
          Write-Host "Stand kit SHA256: $zipSha"
          "zip_path=$zip" >> $env:GITHUB_OUTPUT
          "zip_sha256=$zipSha" >> $env:GITHUB_OUTPUT
          "artifact_name=APL-WIN-014-0.2.4-stand-kit" >> $env:GITHUB_OUTPUT

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-appcontrol-candidate-failure-${{ github.run_id }}
          if-no-files-found: ignore
          path: |
            out/windows-appcontrol-candidate-e2e.json
            out/*appcontrol*/**/*.json
            out/app-control-provisional-trust/**
            out/historical-0.2.2-p0.4-onefile-runtime/onefile-runtime.json
            ${{ runner.temp }}/apl-appcontrol-candidate-e2e-*/logs/*.log

      - name: Upload candidate bundle, provisional policy and lifecycle evidence
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.candidate.outputs.bundle_name }}
          path: |
            ${{ steps.candidate.outputs.bundle_zip }}
            out/windows-appcontrol-candidate-e2e.json
            out/*appcontrol*/**/*.json
            out/app-control-provisional-trust/**

      - name: Upload self-contained ARVECTUM-DEMO stand kit
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.standkit.outputs.artifact_name }}
          path: ${{ steps.standkit.outputs.zip_path }}
